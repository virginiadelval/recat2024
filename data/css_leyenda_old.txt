
		/* Estilos CSS para la leyenda
.info.legend {
    background: white;
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 5px;
    font-size: 12px;
    display: grid;
    grid-template-columns: repeat(10, 1fr); /* Crea 5 columnas */
    grid-gap: 1px; /* Espacio entre los elementos */
   align-items: center;
    text-align: center;

	/*flex-wrap: wrap; /* Permite que los elementos se ajusten en filas */
   /* justify-content: space-between; /* Espacio equitativo entre los elementos */

}

/* Estilos para los círculos de colores en la leyenda */
.info.legend i {
    width: 8px; /* Reducir el tamaño del círculo */
    height: 8px;/* Reducir el tamaño del círculo */
    display: inline-block;
    border-radius: 50%;
    margin-right: 2px; /* Reducir el marge del círculo */
}

/* Estilos para los elementos de la leyenda (cada uno ocupa parte igual) */
.info.legend .legend-item {
	display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column; /* Organiza los números debajo de los círculos */
    font-size: 10px; /* Ajusta el tamaño de fuente si es necesario */
	margin: 0; /* Elimina márgenes adicionales */
    padding: 0; /* Elimina padding adicional*/
}
/*flex-basis: calc(33.33% - 5px); /* Cada elemento ocupa 1/3 del ancho, con espacio entre ellos */
/*margin-bottom: 5px;*/

		/* remove label background elements */
		.leaflet-tooltip.my-labels {
			background-color: transparent;
			border: transparent;
			box-shadow: none;
			font-weight: bold;
			font-size: 30px;
		} */